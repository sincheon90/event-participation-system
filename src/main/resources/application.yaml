spring:
  application:
    name: event-participation-system

  datasource:
    url: jdbc:mysql://localhost:3306/event_participation
    username: app
    password: app

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  data:
    redis:
      host: localhost
      port: 6379
      connect-timeout: 1s
      timeout: 1s